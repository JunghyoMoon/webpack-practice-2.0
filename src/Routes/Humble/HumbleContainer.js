import React, { Component } from "react";
import HumblePresenter from "./HumblePresenter";

class HumbleContainer extends Component {
    render() {
        return <HumblePresenter />;
    }
}

export default HumbleContainer;
